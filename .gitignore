node_modules/
# Upstream response cache — ~3 MB of Parsoid HTML + Wikidata claims per book.
# Regenerated on demand; never committed.
.cache/
*.log
.DS_Store

# User libraries. Never a build artefact, never committed.
data/

# Generated handoff — regenerates in ~2s, so it is never committed and never stale.
STATE.md
